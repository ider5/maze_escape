{
    // 使用 Code Runner 插件在外部终端运行 Rust 代码
    "code-runner.runInTerminal": true,
    "code-runner.executorMap": {
        "rust": "start cmd /k \"cd /d $dir && cargo run\""
    },

    // 文件自动保存
    "files.autoSave": "afterDelay",

    // 让 rust-analyzer 使用 VS Code 的内置调试功能
    // (这是默认行为，通常不需要显式设置)
    // 如果你发现 rust-analyzer 的 "Run" 按钮行为不符合预期，
    // 可以检查 rust-analyzer 的相关设置。

    // 移除其他多余的终端设置，除非你有特殊需求。
}